{
  "//": "No DefaultConnection on purpose. Program.cs selects Npgsql on the presence of 'Host=', so a placeholder pointing at localhost shipped as a REAL instruction to connect to a database inside the container — which does not exist, so startup failed. Set DATABASE_URL in the hosting environment (Render dashboard) to use PostgreSQL; with nothing set the service falls back to local SQLite.",
  "ConnectionStrings": {}
}
